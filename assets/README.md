`screenshot_dashboard.png` is a placeholder — replace it with a real screenshot after
launching the app (the dashboard renders at http://127.0.0.1:8765).
No `app.ico` is bundled; drop one here and re-run `scripts\create_shortcut.ps1` to use it.

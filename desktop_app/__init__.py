"""Local desktop launcher + browser UI for proposal_engine (usability layer only).

This package does not change any generation logic; it drives the existing
pipeline via ``desktop_app.jobs`` and serves a small local FastAPI UI.
"""
